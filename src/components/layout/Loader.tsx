const Loader = () => {
  return (
    <div className={`my-10 flex items-center justify-center`}>
      <div
        className={`h-12 w-12 animate-spin rounded-full border-4 border-solid border-primary border-t-transparent text-center`}
      ></div>
    </div>
  );
};

export default Loader;
